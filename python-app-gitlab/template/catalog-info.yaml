apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: An example of a ${{values.app_name}} application.
  annotations:
    gitlab.com/project-slug: tyrelorde.fecha/${{values.app_name}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental
