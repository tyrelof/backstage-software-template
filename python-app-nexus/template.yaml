apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: python-template-for-nexus
  title: Python flask template For NEXUS
  description: Provisions a minimalistic flask application
spec:
  owner: development
  type: service

  parameters:
    - title: Fill in some steps
      required: [component_id, environment]
      properties:
        component_id:
          title: Component name
          type: string
          description: Provide a component (microservice) name
          pattern: '^([a-zA-Z][a-zA-Z0-9]*)(-[a-zA-Z0-9]+)*$'
        environment:
          title: Environment
          type: string
          enum: [dev, prod]
        ownerPath:
          title: GitLab owner (group/subgroup or username)
          description: e.g. platform or platform/tools or group_17_bot
          type: string
          default: creative-projects
        gitlabHost:
          title: GitLab Host
          type: string
          default: nexus.nmscreative.com

  steps:
    - id: fetch-base
      name: Fetch Base
      action: fetch:template
      input:
        url: ./template
        values:
          app_name: ${{ parameters.component_id }}
          app_env: ${{ parameters.environment }}

    - id: publish
      name: Publish
      action: publish:gitlab
      input:
        # repoUrl: nexus.nmscreative.com?owner=creative-project&repo=${{ parameters.component_id }}
        repoUrl: ${{ parameters.gitlabHost }}?owner=${{ parameters.ownerPath }}&repo=${{ parameters.component_id }}
        repoVisibility: public
        defaultBranch: main

    - id: create-push-token
      name: Create project access token
      action: gitlab:projectAccessToken:create
      input:
        repoUrl: nexus.nmscreative.com?owner=creative-projects&repo=${{ parameters.component_id }}
        name: ci-push
        role: maintainer
        scopes: [write_repository]   # repo-scoped, not a personal token

    - id: save-token
      name: Store token as CI variable
      action: gitlab:projectVariable:create
      input:
        repoUrl: nexus.nmscreative.com?owner=creative-projects&repo=${{ parameters.component_id }}
        key: PROJECT_PUSH_TOKEN
        value: ${{ steps['create-push-token'].output.token }}
        masked: true
        variableProtected: true

    - id: register
      name: Register
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['publish'].output.remoteUrl }}
