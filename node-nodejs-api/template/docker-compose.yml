version: "3.9"

services:
  api:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    command: sh -lc "npm ci && npm run dev"
