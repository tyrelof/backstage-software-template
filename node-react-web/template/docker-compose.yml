version: "3.9"

services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    environment:
      NODE_ENV: development
    command: sh -lc "npm install --prefer-offline --no-audit && npm run dev -- --host 0.0.0.0 --port 5173"
